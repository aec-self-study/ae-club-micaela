
version: 2

models:
  - name: customers
    description: "first week sql"
    columns:
      - name: customer_id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null

  - name: monthly_customers
    description: "second week SQL"
    columns:
      - name: customer_id
        data_tests:
          - unique
          - not_null
